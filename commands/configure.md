---
description: Switch provider and models (anthropic, openai, or custom)
---

Run this exact command and show the output:

```bash
REPO=$(dirname "$(dirname "$(readlink ~/.config/opencode/commands/configure.md)")") && cd "$REPO" && ./configure.sh $ARGUMENTS
```
